<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="chat.css">
    <title>Chatter App</title>
  
</head>
<body>
    <h2>All chats</h2>
    <form action="/chats/new" ><button>Add Chat</button></form>
    
<%  for (let chat of chats) { %>
    <div>
        <p>from : @<%= chat.from %></p>
        <div>
           <i> message :</i> <%= chat.message %>
        </div>
        <p>Rceiver : @<%= chat.to %></p>
        <br>
        <p> <%= chat.created_at.toString().split(" ").slice(0, 4).join("-")%> &nbsp;&nbsp; <%= chat.created_at.toString().split(" ")[4] %></p>
        <hr>
        <form action="/chats/<%= chat._id %>/edit" method="get">
            <button>Edit</button>
        </form>
        <form action="/chats/<%= chat._id %>?_method=DELETE" method="post"> 
            <button>Delete</button>
        </form>
    </div>
 <% } %>
</body>
</html>
